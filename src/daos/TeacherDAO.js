const Teachers = require('../models/Teachers');

export default {
  // getOneUser: () => {
  //   return new Promise((resolve, reject) => {
  //     Teachers.findOne().then(user => {
  //       console.log('user value is '+JSON.stringify(user));
  //       resolve(user);
  //     }).catch(err => {
  //       console.log('error occurred', err);
  //       reject(err);
  //     });
  //   });
  // }
};